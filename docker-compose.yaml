services:
  student-management-system:
    build: .
    environment:
      - DISPLAY=host.docker.internal:0
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
    networks:
      - app-network

networks:
  app-network:
    driver: bridge